# URL backend Laravel (untuk integrasi Pengaturan Akun admin).
# Salin ke .env.local dan isi dengan URL server Laravel (mis. http://localhost:8000).
# Jika tidak diisi, admin tetap pakai data localStorage.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
